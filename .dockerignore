# Keep the build context small and reproducible.
node_modules
dist
coverage
playwright-report
test-results

# VCS / CI / IDE / agent scratch
.git
.github
.claude
.vscode
.idea

# Local env & logs (never bake secrets into the image)
.env
.env.*
!.env.example
*.log
npm-debug.log*
.DS_Store
